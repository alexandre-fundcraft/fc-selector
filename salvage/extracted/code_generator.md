# `code_generator`

## Module docstring

```
Code generator for SPECKIT-008: Auto-Generate OData Serializers.

This module provides utilities to generate syntactically valid Python serializer code
from model metadata.
```

## API surface

- `generate_imports(model_class, app_label)`
  > Generate import statements for the serializer. Args: model_class: Django model class app_label: App label for the model Returns: String containing import statements
- `generate_regeneration_command(app_label, model_name, single)`
  > Generate appropriate regeneration command based on context. Args: app_label: App label for the model model_name: Model name single: Whether the file was generated with --single option Returns: String containing the regeneration command
- `generate_fields_list(fields)`
  > Generate the fields list for Meta class. Args: fields: List of FieldInfo objects Returns: String containing Python list with proper indentation
- `generate_expandable_fields(relationships, app_label, exclude_edges, models_in_file)`
  > Generate the expandable_fields dict for Meta class. Args: relationships: List of RelationshipInfo objects app_label: App label for the model exclude_edges: Set of edges to exclude from expandable_fields models_in_file: Set of model paths being generated in the same file (for single mode) Returns: St
- `generate_serializer_class(model_class, app_label, fields, relationships, exclude_edges, single, models_in_file)`
  > Generate complete serializer class code. Args: model_class: Django model class app_label: App label for the model fields: List of FieldInfo objects relationships: List of RelationshipInfo objects exclude_edges: Set of edges to exclude single: Whether the file was generated with --single option model
- `format_python_code(code)`
  > Format and validate Python code. Args: code: Python code string Returns: Formatted Python code Raises: SyntaxError: If code is not valid Python

## String constants

Templates and messages, in definition order.

### in `generate_imports`

```
from django_odata.serializers import ODataModelSerializer
```

### in `generate_imports`

```
from django.apps import apps
```

### in `generate_imports`

```


```

### in `generate_regeneration_command`

```
python manage.py generate_odata_serializer 
```

### in `generate_fields_list`

```
[

```

### in `generate_fields_list`

```

        ]
```

### in `generate_serializer_class`

```
"""
Auto-generated OData serializer for 
```

### in `generate_serializer_class`

```
 model.
Generated on: 
```

### in `generate_serializer_class`

```


DO NOT EDIT THIS FILE MANUALLY.
Regenerate using: 
```

### in `generate_serializer_class`

```


Available options:
  --single   Generate one combined file instead of separate files
  --force    Overwrite existing files without prompting
"""

```

### in `generate_serializer_class`

```

class 
```

### in `generate_serializer_class`

```
(ODataModelSerializer):
    """OData serializer for 
```

### in `generate_serializer_class`

```
 model."""

    class Meta:
        model = 
```

### in `generate_serializer_class`

```

        fields = 
```

### in `generate_serializer_class`

```

        expandable_fields = 
```

### in `format_python_code`

```
Generated code has syntax error: 
```

